# PayHug 회원관리 개편 — 공유 배포

작업 레포(비공개 `Joo2n/payhug-member-redesign`)의 공유용 미러. 직접 수정하지 않는다 — 갱신은 작업 레포에서 `./deploy_hub.sh` 실행.

- `index.html` 허브(진입점) / `v2.html` 프로토타입 기준판 / `v1.html` 라디오 비교판 / `mockups.html` 목업 갤러리 / 구조서 HTML
- 요율 숫자는 전부 예시 값(실제 수수료율 미확정)
